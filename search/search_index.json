{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"FastAPI FullAuth","text":"FastAPIFullAuth <p>Ready-to-use, async-native authentication and authorization for FastAPI.</p> <p>Documentation: https://mdfarhankc.github.io/fastapi-fullauth</p> <p>Source Code: https://github.com/mdfarhankc/fastapi-fullauth</p> <p>Add a complete authentication and authorization system to your FastAPI project. FastAPI FullAuth is designed to be production-ready, async-native, and pluggable \u2014 handling JWT tokens, refresh rotation, password hashing, email verification, OAuth2 social login, and role-based access out of the box.</p>"},{"location":"#features","title":"Features","text":"<ul> <li>JWT access + refresh tokens with configurable expiry</li> <li>Refresh token rotation with reuse detection (revokes entire session family on replay)</li> <li>Password hashing via Argon2id (default) or bcrypt</li> <li>Email verification and password reset flows with event hooks</li> <li>OAuth2 social login \u2014 Google and GitHub, with multi-redirect-URI support</li> <li>Role-based access control \u2014 <code>CurrentUser</code>, <code>VerifiedUser</code>, <code>SuperUser</code>, <code>require_role()</code></li> <li>Rate limiting \u2014 per-route auth limits + global middleware (memory or Redis)</li> <li>CSRF protection and security headers middleware</li> <li>Pluggable adapters \u2014 SQLModel, SQLAlchemy, or in-memory</li> <li>Auto-derived schemas \u2014 custom user fields are picked up automatically</li> <li>Event hooks \u2014 <code>after_register</code>, <code>after_login</code>, <code>send_verification_email</code>, etc.</li> <li>Custom JWT claims \u2014 embed app-specific data in tokens</li> <li>Redis support \u2014 token blacklist and rate limiter backends</li> <li>Python 3.10 -- 3.14 supported</li> </ul>"},{"location":"#installation","title":"Installation","text":"<pre><code>pip install fastapi-fullauth\n\n# with an ORM adapter\npip install fastapi-fullauth[sqlmodel]\npip install fastapi-fullauth[sqlalchemy]\n\n# with redis for token blacklisting\npip install fastapi-fullauth[sqlmodel,redis]\n\n# with OAuth2 social login\npip install fastapi-fullauth[sqlmodel,oauth]\n\n# everything\npip install fastapi-fullauth[all]\n</code></pre>"},{"location":"#example","title":"Example","text":"<pre><code>from fastapi import FastAPI\nfrom fastapi_fullauth import FullAuth\nfrom fastapi_fullauth.adapters.memory import InMemoryAdapter\n\napp = FastAPI()\n\nfullauth = FullAuth(\n    secret_key=\"your-secret-key\",\n    adapter=InMemoryAdapter(),\n)\nfullauth.init_app(app)\n</code></pre> <p>This gives you <code>/auth/register</code>, <code>/auth/login</code>, <code>/auth/logout</code>, <code>/auth/refresh</code>, <code>/auth/me</code>, <code>/auth/change-password</code>, <code>/auth/password-reset/*</code>, <code>/auth/verify-email/*</code>, and admin role management \u2014 all under <code>/api/v1</code> by default.</p> <p>Omit <code>secret_key</code> in dev and a random one is generated (tokens won't survive restarts).</p>"},{"location":"#routes","title":"Routes","text":"Method Path Description <code>POST</code> <code>/auth/register</code> Create a new user <code>POST</code> <code>/auth/login</code> Authenticate, get tokens <code>POST</code> <code>/auth/logout</code> Blacklist token <code>POST</code> <code>/auth/refresh</code> Rotate token pair <code>GET</code> <code>/auth/me</code> Get current user <code>GET</code> <code>/auth/me/verified</code> Verified users only <code>PATCH</code> <code>/auth/me</code> Update profile <code>DELETE</code> <code>/auth/me</code> Delete account <code>POST</code> <code>/auth/change-password</code> Change password <code>POST</code> <code>/auth/verify-email/request</code> Request verification email <code>POST</code> <code>/auth/verify-email/confirm</code> Confirm email <code>POST</code> <code>/auth/password-reset/request</code> Request password reset <code>POST</code> <code>/auth/password-reset/confirm</code> Reset password <code>POST</code> <code>/auth/admin/assign-role</code> Assign role (superuser) <code>POST</code> <code>/auth/admin/remove-role</code> Remove role (superuser) <code>POST</code> <code>/auth/admin/assign-permission</code> Assign permission to role (superuser) <code>POST</code> <code>/auth/admin/remove-permission</code> Remove permission from role (superuser) <code>GET</code> <code>/auth/admin/role-permissions/{role}</code> List role's permissions (superuser) <p>With OAuth enabled, additional routes are registered under <code>/auth/oauth/</code>. See OAuth2 Social Login.</p> <p>All routes are prefixed with <code>/api/v1</code> by default (configurable via <code>API_PREFIX</code>).</p>"},{"location":"#license","title":"License","text":"<p>MIT</p>"},{"location":"api-reference/","title":"API Reference","text":"<p>Quick reference for the main classes, types, and functions.</p>"},{"location":"api-reference/#fullauth","title":"FullAuth","text":"<p>The main auth manager. Central entry point for the library.</p> <pre><code>from fastapi_fullauth import FullAuth\n\nfullauth = FullAuth(\n    config=None,                    # FullAuthConfig object (mutually exclusive with kwargs)\n    adapter=adapter,                # required \u2014 database adapter\n    secret_key=None,                # shortcut for config.SECRET_KEY\n    backends=None,                  # [BearerBackend()] by default\n    password_validator=None,        # PasswordValidator instance\n    enabled_routes=None,            # whitelist of route names, None = all\n    include_user_in_login=False,    # include user data in login response\n    create_user_schema=None,        # custom registration schema\n    on_create_token_claims=None,    # async callback for custom JWT claims\n    **config_kwargs,                # any FullAuthConfig field as lowercase\n)\n</code></pre>"},{"location":"api-reference/#methods","title":"Methods","text":"Method Description <code>init_app(app, auto_middleware=True)</code> Mount routes and middleware on a FastAPI app <code>hooks.on(event, callback)</code> Register an event hook"},{"location":"api-reference/#properties","title":"Properties","text":"Property Type Description <code>config</code> <code>FullAuthConfig</code> Active configuration <code>adapter</code> <code>AbstractUserAdapter</code> Database adapter <code>token_engine</code> <code>TokenEngine</code> JWT creation/validation engine <code>router</code> <code>APIRouter</code> The auth router (lazy-created)"},{"location":"api-reference/#fullauthconfig","title":"FullAuthConfig","text":"<pre><code>from fastapi_fullauth import FullAuthConfig\n</code></pre> <p>Pydantic Settings class. See Configuration for all options.</p>"},{"location":"api-reference/#types","title":"Types","text":"<pre><code>from fastapi_fullauth.types import (\n    UserSchema,         # base user response model\n    CreateUserSchema,   # base registration model (email + password)\n    TokenPair,          # access_token + refresh_token + token_type + expires_in\n    TokenPayload,       # decoded JWT payload\n    RefreshToken,       # stored refresh token record\n    OAuthAccount,       # linked OAuth provider account\n    OAuthUserInfo,      # user info from OAuth provider\n    RouteName,          # Literal type of all route names\n)\n</code></pre>"},{"location":"api-reference/#userschema","title":"UserSchema","text":"<pre><code>class UserSchema(BaseModel):\n    id: str | int | UUID\n    email: EmailStr\n    is_active: bool = True\n    is_verified: bool = False\n    is_superuser: bool = False\n    roles: list[str] = []\n</code></pre>"},{"location":"api-reference/#tokenpair","title":"TokenPair","text":"<pre><code>class TokenPair(BaseModel):\n    access_token: str\n    refresh_token: str\n    token_type: str = \"bearer\"\n    expires_in: int | None = None\n</code></pre>"},{"location":"api-reference/#tokenpayload","title":"TokenPayload","text":"<pre><code>class TokenPayload(BaseModel):\n    sub: str              # user ID\n    exp: datetime         # expiry\n    iat: datetime         # issued at\n    jti: str              # unique token ID\n    type: str             # \"access\" or \"refresh\"\n    roles: list[str]      # user roles\n    extra: dict[str, Any] # custom claims\n    family_id: str | None # refresh token family\n</code></pre>"},{"location":"api-reference/#routename","title":"RouteName","text":"<pre><code>RouteName = Literal[\n    \"login\", \"logout\", \"register\", \"refresh\",\n    \"verify-email\", \"password-reset\", \"me\", \"verified-me\",\n    \"change-password\", \"update-profile\", \"delete-account\",\n]\n</code></pre>"},{"location":"api-reference/#dependencies","title":"Dependencies","text":"<pre><code>from fastapi_fullauth.dependencies import (\n    CurrentUser,        # Annotated type \u2014 any authenticated user\n    VerifiedUser,       # Annotated type \u2014 verified email required\n    SuperUser,          # Annotated type \u2014 superuser required\n    current_user,       # function form of CurrentUser\n    require_role,       # require_role(\"admin\", \"editor\")\n    require_permission, # require_permission(\"posts:edit\", \"posts:delete\")\n)\n</code></pre>"},{"location":"api-reference/#exceptions","title":"Exceptions","text":"<pre><code>from fastapi_fullauth.exceptions import (\n    FullAuthError,                  # base exception\n    AuthenticationError,            # login failed\n    AuthorizationError,             # insufficient permissions\n    TokenError,                     # invalid token\n    TokenExpiredError,              # token expired\n    TokenBlacklistedError,          # token was revoked\n    UserAlreadyExistsError,         # duplicate registration\n    UserNotFoundError,              # user not found\n    InvalidPasswordError,           # password validation failed\n    AccountLockedError,             # too many failed attempts\n    OAuthError,                     # OAuth base error\n    OAuthProviderError,             # provider-specific error\n)\n</code></pre>"},{"location":"api-reference/#utilities","title":"Utilities","text":"<pre><code>from fastapi_fullauth import generate_secret_key, create_superuser\n\n# generate a cryptographically secure secret key\nkey = generate_secret_key()\n\n# create a superuser programmatically\nuser = await create_superuser(adapter, \"admin@example.com\", \"password\")\n</code></pre>"},{"location":"api-reference/#validators","title":"Validators","text":"<pre><code>from fastapi_fullauth import PasswordValidator\n\nvalidator = PasswordValidator(\n    min_length=10,\n    require_uppercase=True,\n    require_lowercase=True,\n    require_digit=True,\n    require_special=True,\n    blocked_passwords=[\"password123\"],\n)\n</code></pre>"},{"location":"configuration/","title":"Configuration","text":"<p>All configuration is managed through <code>FullAuthConfig</code>, a Pydantic Settings class. Every option can be set via environment variables with the <code>FULLAUTH_</code> prefix.</p>"},{"location":"configuration/#usage","title":"Usage","text":"<p>Pass config inline or as an object:</p> Inline kwargsConfig objectEnvironment variables <pre><code>fullauth = FullAuth(\n    secret_key=\"...\",\n    adapter=adapter,\n    access_token_expire_minutes=60,\n    api_prefix=\"/api/v2\",\n)\n</code></pre> <pre><code>from fastapi_fullauth import FullAuthConfig\n\nconfig = FullAuthConfig(\n    SECRET_KEY=\"...\",\n    ACCESS_TOKEN_EXPIRE_MINUTES=60,\n    API_PREFIX=\"/api/v2\",\n)\nfullauth = FullAuth(config=config, adapter=adapter)\n</code></pre> <pre><code>export FULLAUTH_SECRET_KEY=\"your-secret-key\"\nexport FULLAUTH_ACCESS_TOKEN_EXPIRE_MINUTES=60\n</code></pre> <pre><code># reads from env automatically\nfullauth = FullAuth(adapter=adapter)\n</code></pre>"},{"location":"configuration/#reference","title":"Reference","text":""},{"location":"configuration/#core","title":"Core","text":"Option Type Default Description <code>SECRET_KEY</code> <code>str \\| None</code> <code>None</code> JWT signing key. Auto-generated in dev if not set. <code>ALGORITHM</code> <code>str</code> <code>\"HS256\"</code> JWT signing algorithm."},{"location":"configuration/#tokens","title":"Tokens","text":"Option Type Default Description <code>ACCESS_TOKEN_EXPIRE_MINUTES</code> <code>int</code> <code>30</code> Access token lifetime. <code>REFRESH_TOKEN_EXPIRE_DAYS</code> <code>int</code> <code>30</code> Refresh token lifetime. <code>REFRESH_TOKEN_ROTATION</code> <code>bool</code> <code>True</code> Issue new refresh token on each refresh."},{"location":"configuration/#passwords","title":"Passwords","text":"Option Type Default Description <code>PASSWORD_HASH_ALGORITHM</code> <code>\"argon2id\" \\| \"bcrypt\"</code> <code>\"argon2id\"</code> Hashing algorithm. <code>PASSWORD_MIN_LENGTH</code> <code>int</code> <code>8</code> Minimum password length."},{"location":"configuration/#login","title":"Login","text":"Option Type Default Description <code>LOGIN_FIELD</code> <code>str</code> <code>\"email\"</code> Field used for login (<code>\"email\"</code>, <code>\"username\"</code>, etc.). <code>MAX_LOGIN_ATTEMPTS</code> <code>int</code> <code>5</code> Failed attempts before account lockout. <code>LOCKOUT_DURATION_MINUTES</code> <code>int</code> <code>15</code> Lockout duration after max attempts."},{"location":"configuration/#rate-limiting","title":"Rate Limiting","text":"Option Type Default Description <code>RATE_LIMIT_ENABLED</code> <code>bool</code> <code>False</code> Enable global rate limit middleware. <code>RATE_LIMIT_BACKEND</code> <code>\"memory\" \\| \"redis\"</code> <code>\"memory\"</code> Rate limiter storage backend. <code>TRUSTED_PROXY_HEADERS</code> <code>list[str]</code> <code>[]</code> Headers to read real client IP from (e.g. <code>[\"X-Forwarded-For\"]</code>). <code>AUTH_RATE_LIMIT_ENABLED</code> <code>bool</code> <code>True</code> Enable per-route auth rate limits. <code>AUTH_RATE_LIMIT_LOGIN</code> <code>int</code> <code>5</code> Max login attempts per window. <code>AUTH_RATE_LIMIT_REGISTER</code> <code>int</code> <code>3</code> Max registrations per window. <code>AUTH_RATE_LIMIT_PASSWORD_RESET</code> <code>int</code> <code>3</code> Max password reset requests per window. <code>AUTH_RATE_LIMIT_WINDOW_SECONDS</code> <code>int</code> <code>60</code> Rate limit window in seconds."},{"location":"configuration/#redis","title":"Redis","text":"Option Type Default Description <code>REDIS_URL</code> <code>str \\| None</code> <code>None</code> Redis connection URL. Required when using Redis backends."},{"location":"configuration/#token-blacklist","title":"Token Blacklist","text":"Option Type Default Description <code>BLACKLIST_ENABLED</code> <code>bool</code> <code>True</code> Check blacklist on token decode. <code>BLACKLIST_BACKEND</code> <code>\"memory\" \\| \"redis\"</code> <code>\"memory\"</code> Blacklist storage backend."},{"location":"configuration/#middleware","title":"Middleware","text":"Option Type Default Description <code>INJECT_SECURITY_HEADERS</code> <code>bool</code> <code>True</code> Auto-add security headers middleware. <code>CSRF_ENABLED</code> <code>bool</code> <code>False</code> Auto-add CSRF middleware. <code>CSRF_SECRET</code> <code>str \\| None</code> <code>None</code> CSRF signing secret. Falls back to <code>SECRET_KEY</code>."},{"location":"configuration/#cookies","title":"Cookies","text":"Option Type Default Description <code>COOKIE_NAME</code> <code>str</code> <code>\"fullauth_access\"</code> Access token cookie name. <code>COOKIE_SECURE</code> <code>bool</code> <code>True</code> Set Secure flag on cookies. <code>COOKIE_HTTPONLY</code> <code>bool</code> <code>True</code> Set HttpOnly flag on cookies. <code>COOKIE_SAMESITE</code> <code>\"lax\" \\| \"strict\" \\| \"none\"</code> <code>\"lax\"</code> SameSite cookie policy. <code>COOKIE_DOMAIN</code> <code>str \\| None</code> <code>None</code> Cookie domain."},{"location":"configuration/#oauth","title":"OAuth","text":"Option Type Default Description <code>OAUTH_PROVIDERS</code> <code>dict</code> <code>{}</code> OAuth provider configurations. See OAuth2. <code>OAUTH_STATE_EXPIRE_SECONDS</code> <code>int</code> <code>300</code> OAuth state token TTL (5 min). <code>OAUTH_AUTO_LINK_BY_EMAIL</code> <code>bool</code> <code>True</code> Auto-link OAuth accounts to existing users by email."},{"location":"configuration/#routing","title":"Routing","text":"Option Type Default Description <code>API_PREFIX</code> <code>str</code> <code>\"/api/v1\"</code> URL prefix for all routes. <code>AUTH_ROUTER_PREFIX</code> <code>str</code> <code>\"/auth\"</code> Auth router sub-prefix. <code>ROUTER_TAGS</code> <code>list[str]</code> <code>[\"Auth\"]</code> OpenAPI tags for auth routes."},{"location":"contributing/","title":"Contributing","text":"<p>Thanks for your interest in contributing to FastAPI FullAuth!</p>"},{"location":"contributing/#development-setup","title":"Development setup","text":"<pre><code>git clone https://github.com/mdfarhankc/fastapi-fullauth.git\ncd fastapi-fullauth\nuv sync --dev --extra sqlalchemy --extra sqlmodel --extra redis --extra oauth\n</code></pre>"},{"location":"contributing/#running-tests","title":"Running tests","text":"<pre><code>uv run pytest tests/ -v\n</code></pre>"},{"location":"contributing/#linting-and-formatting","title":"Linting and formatting","text":"<pre><code>uv run ruff check .\nuv run ruff format .\n</code></pre> <p>Both must pass before submitting a PR. CI enforces this.</p>"},{"location":"contributing/#making-changes","title":"Making changes","text":"<ol> <li>Fork the repo and create a branch from <code>main</code></li> <li>Make your changes</li> <li>Add tests for new functionality</li> <li>Ensure all tests pass and lint is clean</li> <li>Submit a pull request</li> </ol>"},{"location":"contributing/#branch-naming","title":"Branch naming","text":"Prefix Use <code>feat/</code> New features <code>fix/</code> Bug fixes <code>refactor/</code> Code improvements <code>docs/</code> Documentation"},{"location":"contributing/#what-to-contribute","title":"What to contribute","text":"<ul> <li>Bug fixes</li> <li>New OAuth providers (Apple, Discord, Microsoft, etc.)</li> <li>Adapter implementations (MongoDB, Tortoise ORM, etc.)</li> <li>Documentation improvements</li> <li>Test coverage improvements</li> <li>Performance improvements</li> </ul>"},{"location":"contributing/#reporting-bugs","title":"Reporting bugs","text":"<p>Use the bug report template on GitHub Issues.</p>"},{"location":"contributing/#requesting-features","title":"Requesting features","text":"<p>Use the feature request template on GitHub Issues.</p>"},{"location":"contributing/#code-style","title":"Code style","text":"<ul> <li>Follow existing patterns in the codebase</li> <li>Use type annotations</li> <li>Keep functions focused and small</li> <li>Log security-sensitive events via <code>logging.getLogger(\"fastapi_fullauth.*\")</code></li> <li>Don't add docstrings/comments unless the logic isn't self-evident</li> </ul>"},{"location":"contributing/#license","title":"License","text":"<p>By contributing, you agree that your contributions will be licensed under the MIT License.</p>"},{"location":"getting-started/","title":"Getting Started","text":"<p>This guide walks through setting up fastapi-fullauth from scratch.</p>"},{"location":"getting-started/#installation","title":"Installation","text":"<pre><code>pip install fastapi-fullauth[sqlmodel]\n</code></pre>"},{"location":"getting-started/#1-define-your-user-model","title":"1. Define your user model","text":"<pre><code># models.py\nfrom sqlmodel import Field, Relationship\nfrom fastapi_fullauth.adapters.sqlmodel import (\n    UserBase, Role, UserRoleLink, RefreshTokenRecord,\n)\n\nclass User(UserBase, table=True):\n    __tablename__ = \"fullauth_users\"\n\n    display_name: str = Field(default=\"\", max_length=100)\n    phone: str = Field(default=\"\", max_length=20)\n\n    roles: list[Role] = Relationship(link_model=UserRoleLink)\n    refresh_tokens: list[RefreshTokenRecord] = Relationship()\n</code></pre> <p><code>UserBase</code> provides <code>id</code>, <code>email</code>, <code>hashed_password</code>, <code>is_active</code>, <code>is_verified</code>, <code>is_superuser</code>, and <code>created_at</code>. Add any extra fields you need.</p> <p>Note</p> <p>Registration and response schemas are auto-derived from your model. <code>display_name</code> and <code>phone</code> will appear in the register endpoint and user responses automatically.</p>"},{"location":"getting-started/#2-set-up-the-database","title":"2. Set up the database","text":"<pre><code># config.py\nfrom sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine\n\nDATABASE_URL = \"sqlite+aiosqlite:///app.db\"\nengine = create_async_engine(DATABASE_URL)\nsession_maker = async_sessionmaker(engine, expire_on_commit=False)\n</code></pre>"},{"location":"getting-started/#3-configure-fullauth","title":"3. Configure FullAuth","text":"<pre><code># auth.py\nfrom fastapi_fullauth import FullAuth\nfrom fastapi_fullauth.adapters.sqlmodel import SQLModelAdapter\n\nfrom .config import session_maker\nfrom .models import User\n\nfullauth = FullAuth(\n    secret_key=\"your-secret-key-at-least-32-bytes\",\n    adapter=SQLModelAdapter(session_maker=session_maker, user_model=User),\n)\n</code></pre> <p>Tip</p> <p>Omit <code>secret_key</code> during development and a random one is generated automatically. Tokens won't survive restarts, but it's convenient for dev.</p>"},{"location":"getting-started/#4-wire-it-into-fastapi","title":"4. Wire it into FastAPI","text":"<pre><code># main.py\nfrom contextlib import asynccontextmanager\nfrom fastapi import FastAPI\nfrom sqlmodel import SQLModel\n\nfrom .auth import fullauth\nfrom .config import engine\n\n@asynccontextmanager\nasync def lifespan(app: FastAPI):\n    async with engine.begin() as conn:\n        await conn.run_sync(SQLModel.metadata.create_all)\n    yield\n    await engine.dispose()\n\napp = FastAPI(lifespan=lifespan)\nfullauth.init_app(app)\n</code></pre> <p>That's it. Start the server and you have a full auth system:</p> <pre><code>uvicorn main:app --reload\n</code></pre>"},{"location":"getting-started/#5-try-it-out","title":"5. Try it out","text":"<p>Register:</p> <pre><code>curl -X POST http://localhost:8000/api/v1/auth/register \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"email\": \"user@example.com\", \"password\": \"securepass123\"}'\n</code></pre> <p>Login:</p> <pre><code>curl -X POST http://localhost:8000/api/v1/auth/login \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"email\": \"user@example.com\", \"password\": \"securepass123\"}'\n</code></pre> <p>Response:</p> <pre><code>{\n  \"access_token\": \"eyJ...\",\n  \"refresh_token\": \"eyJ...\",\n  \"token_type\": \"bearer\",\n  \"expires_in\": 1800\n}\n</code></pre> <p>Get current user:</p> <pre><code>curl http://localhost:8000/api/v1/auth/me \\\n  -H \"Authorization: Bearer eyJ...\"\n</code></pre>"},{"location":"getting-started/#6-add-protected-routes","title":"6. Add protected routes","text":"<pre><code>from fastapi_fullauth.dependencies import CurrentUser, VerifiedUser, require_role\n\n@app.get(\"/profile\")\nasync def profile(user: CurrentUser):\n    return user\n\n@app.get(\"/dashboard\")\nasync def dashboard(user: VerifiedUser):\n    return {\"email\": user.email}\n\n@app.get(\"/admin\")\nasync def admin(user=Depends(require_role(\"admin\"))):\n    return {\"msg\": \"admin area\"}\n</code></pre> <p>See Protected Routes for all dependency types.</p>"},{"location":"getting-started/#next-steps","title":"Next steps","text":"<ul> <li>Configuration \u2014 all config options</li> <li>OAuth2 Social Login \u2014 add Google/GitHub login</li> <li>Event Hooks \u2014 send emails, log events</li> <li>Rate Limiting \u2014 protect your endpoints</li> </ul>"},{"location":"migrations/","title":"Database Migrations","text":"<p>fastapi-fullauth provides Alembic integration helpers for managing database schema migrations.</p>"},{"location":"migrations/#quick-start-without-alembic","title":"Quick start (without Alembic)","text":"<p>For development or simple projects, create tables directly:</p> SQLModelSQLAlchemy <pre><code>from sqlmodel import SQLModel\n\nasync with engine.begin() as conn:\n    await conn.run_sync(SQLModel.metadata.create_all)\n</code></pre> <pre><code>from fastapi_fullauth.adapters.sqlalchemy.models import FullAuthBase\n\nasync with engine.begin() as conn:\n    await conn.run_sync(FullAuthBase.metadata.create_all)\n</code></pre>"},{"location":"migrations/#alembic-integration","title":"Alembic integration","text":"<p>For production, use Alembic for proper migration management.</p>"},{"location":"migrations/#1-initialize-alembic","title":"1. Initialize Alembic","text":"<pre><code>alembic init alembic\n</code></pre>"},{"location":"migrations/#2-update-envpy","title":"2. Update env.py","text":"<p>Import fullauth models so Alembic detects them during autogenerate:</p> SQLModelSQLAlchemy <pre><code># alembic/env.py\nfrom fastapi_fullauth.migrations import include_fullauth_models\nfrom sqlmodel import SQLModel\n\ninclude_fullauth_models(\"sqlmodel\")\n\n# import your app's models too\nfrom your_app.models import User  # noqa: F401\n\ntarget_metadata = SQLModel.metadata\n</code></pre> <pre><code># alembic/env.py\nfrom fastapi_fullauth.migrations import include_fullauth_models\nfrom your_app.models import Base  # your declarative base\n\ninclude_fullauth_models(\"sqlalchemy\")\ntarget_metadata = Base.metadata\n</code></pre>"},{"location":"migrations/#3-generate-migrations","title":"3. Generate migrations","text":"<pre><code>alembic revision --autogenerate -m \"add fullauth tables\"\nalembic upgrade head\n</code></pre>"},{"location":"migrations/#tables-created","title":"Tables created","text":"Table Description <code>fullauth_users</code> User accounts (your model extends <code>UserBase</code>) <code>fullauth_roles</code> Role definitions <code>fullauth_user_roles</code> User-role many-to-many link <code>fullauth_refresh_tokens</code> Stored refresh tokens with family tracking <code>fullauth_oauth_accounts</code> Linked OAuth provider accounts"},{"location":"migrations/#helper-functions","title":"Helper functions","text":""},{"location":"migrations/#include_fullauth_modelsadapter","title":"<code>include_fullauth_models(adapter)</code>","text":"<p>Imports fullauth model classes so Alembic's autogenerate detects them. Call this in <code>env.py</code> before setting <code>target_metadata</code>.</p> <pre><code>from fastapi_fullauth.migrations import include_fullauth_models\ninclude_fullauth_models(\"sqlmodel\")  # or \"sqlalchemy\"\n</code></pre>"},{"location":"migrations/#get_fullauth_metadataadapter","title":"<code>get_fullauth_metadata(adapter)</code>","text":"<p>Returns the SQLAlchemy <code>MetaData</code> object containing fullauth table definitions. Useful if you need to merge metadata from multiple sources.</p> <pre><code>from fastapi_fullauth.migrations import get_fullauth_metadata\nmetadata = get_fullauth_metadata(\"sqlalchemy\")\n</code></pre>"},{"location":"oauth/","title":"OAuth2 Social Login","text":"<p>Add Google and GitHub login with a few config lines. Users can link multiple providers alongside email/password login.</p>"},{"location":"oauth/#installation","title":"Installation","text":"<pre><code>pip install fastapi-fullauth[oauth]\n</code></pre>"},{"location":"oauth/#configuration","title":"Configuration","text":"<pre><code>fullauth = FullAuth(\n    secret_key=\"...\",\n    adapter=adapter,\n    oauth_providers={\n        \"google\": {\n            \"client_id\": \"your-google-client-id\",\n            \"client_secret\": \"your-google-secret\",\n            \"redirect_uris\": [\n                \"http://localhost:3000/auth/callback\",\n                \"https://myapp.com/auth/callback\",\n            ],\n        },\n        \"github\": {\n            \"client_id\": \"your-github-client-id\",\n            \"client_secret\": \"your-github-secret\",\n            \"redirect_uri\": \"http://localhost:3000/auth/callback\",\n        },\n    },\n)\n</code></pre> <p>Tip</p> <p>Use <code>redirect_uris</code> (list) for multiple allowed callback URLs, or <code>redirect_uri</code> (string) for a single one.</p>"},{"location":"oauth/#routes","title":"Routes","text":"<p>When OAuth providers are configured, these routes are registered automatically:</p> Method Path Description GET <code>/auth/oauth/providers</code> List configured providers GET <code>/auth/oauth/{provider}/authorize</code> Get authorization URL POST <code>/auth/oauth/{provider}/callback</code> Exchange code for tokens GET <code>/auth/oauth/accounts</code> List linked OAuth accounts DELETE <code>/auth/oauth/accounts/{provider}</code> Unlink a provider"},{"location":"oauth/#how-the-flow-works","title":"How the flow works","text":""},{"location":"oauth/#1-get-the-authorization-url","title":"1. Get the authorization URL","text":"<pre><code>GET /api/v1/auth/oauth/google/authorize?redirect_uri=http://localhost:3000/auth/callback\n</code></pre> <p>Response:</p> <pre><code>{\n  \"authorization_url\": \"https://accounts.google.com/o/oauth2/v2/auth?client_id=...&amp;state=...\"\n}\n</code></pre> <p>The <code>redirect_uri</code> parameter is optional. If omitted, the first URI in your <code>redirect_uris</code> list is used. The value is validated against the allowed list.</p>"},{"location":"oauth/#2-redirect-the-user","title":"2. Redirect the user","text":"<p>Your frontend redirects the user to the <code>authorization_url</code>. The user authenticates with Google/GitHub.</p>"},{"location":"oauth/#3-handle-the-callback","title":"3. Handle the callback","text":"<p>The provider redirects back to your <code>redirect_uri</code> with <code>code</code> and <code>state</code> query parameters. Your frontend sends these to the callback endpoint:</p> <pre><code>POST /api/v1/auth/oauth/google/callback\n{\n  \"code\": \"4/0AX4XfW...\",\n  \"state\": \"eyJ...\"\n}\n</code></pre> <p>Response:</p> <pre><code>{\n  \"access_token\": \"eyJ...\",\n  \"refresh_token\": \"eyJ...\",\n  \"token_type\": \"bearer\",\n  \"expires_in\": 1800\n}\n</code></pre> <p>From this point on, the session works exactly like email/password login. The user can call <code>/me</code>, <code>/refresh</code>, <code>/logout</code>, etc. with the JWT tokens.</p>"},{"location":"oauth/#what-happens-on-callback","title":"What happens on callback","text":"<ol> <li>State token is verified (CSRF protection, 5-minute TTL)</li> <li>Authorization code is exchanged for provider tokens</li> <li>User info is fetched from the provider (email, name, picture)</li> <li>Account linking logic runs:<ul> <li>If this provider account is already linked \u2192 update tokens, return existing user</li> <li>If an account with the same email exists \u2192 link the OAuth account to it</li> <li>Otherwise \u2192 create a new user with a random password</li> </ul> </li> <li>JWT tokens are issued (same as regular login)</li> </ol> <p>Note</p> <p>If the provider reports the email as verified, the user's <code>is_verified</code> flag is set to <code>True</code> automatically.</p>"},{"location":"oauth/#auto-linking-by-email","title":"Auto-linking by email","text":"<p>By default, if a user registers with <code>user@example.com</code> via email/password, then later logs in with Google using the same email, the accounts are linked automatically. Disable this with:</p> <pre><code>fullauth = FullAuth(\n    ...\n    oauth_auto_link_by_email=False,\n)\n</code></pre>"},{"location":"oauth/#unlinking-providers","title":"Unlinking providers","text":"<p>Users can unlink an OAuth provider:</p> <pre><code>DELETE /api/v1/auth/oauth/accounts/google\n</code></pre> <p>This is blocked if the OAuth account is the user's only login method (no password set, no other OAuth providers). The user must set a password first.</p>"},{"location":"oauth/#event-hooks","title":"Event hooks","text":"<pre><code>async def on_oauth_login(user, provider, is_new_user):\n    if is_new_user:\n        print(f\"New user via {provider}: {user.email}\")\n    else:\n        print(f\"Returning user via {provider}: {user.email}\")\n\nfullauth.hooks.on(\"after_oauth_login\", on_oauth_login)\n</code></pre> <p>The <code>after_register</code> hook also fires for new OAuth users.</p>"},{"location":"oauth/#provider-setup-guides","title":"Provider setup guides","text":""},{"location":"oauth/#google","title":"Google","text":"<ol> <li>Go to Google Cloud Console</li> <li>Create a project (or select existing)</li> <li>Go to APIs &amp; Services &gt; Credentials</li> <li>Create an OAuth 2.0 Client ID (Web application)</li> <li>Add your redirect URIs under Authorized redirect URIs</li> <li>Copy the Client ID and Client Secret</li> </ol> <p>Default scopes: <code>openid</code>, <code>email</code>, <code>profile</code></p>"},{"location":"oauth/#github","title":"GitHub","text":"<ol> <li>Go to GitHub Developer Settings</li> <li>Click New OAuth App</li> <li>Set the Authorization callback URL to your redirect URI</li> <li>Copy the Client ID and Client Secret</li> </ol> <p>Default scopes: <code>read:user</code>, <code>user:email</code></p>"},{"location":"adapters/","title":"Adapters","text":"<p>Adapters are the database layer for fastapi-fullauth. They implement <code>AbstractUserAdapter</code>, which defines how users, refresh tokens, roles, and OAuth accounts are stored and retrieved.</p>"},{"location":"adapters/#available-adapters","title":"Available adapters","text":"Adapter Backend Install SQLModel Any SQLAlchemy-supported DB <code>pip install fastapi-fullauth[sqlmodel]</code> SQLAlchemy Any SQLAlchemy-supported DB <code>pip install fastapi-fullauth[sqlalchemy]</code> In-Memory Python dicts Included (no extras)"},{"location":"adapters/#choosing-an-adapter","title":"Choosing an adapter","text":"<ul> <li>SQLModel \u2014 recommended for most projects. Clean model definitions, good type support.</li> <li>SQLAlchemy \u2014 use if your project already uses SQLAlchemy's declarative base.</li> <li>In-Memory \u2014 for testing and prototyping only. Data is lost on restart.</li> </ul>"},{"location":"adapters/#custom-adapters","title":"Custom adapters","text":"<p>You can implement your own adapter for any database by subclassing <code>AbstractUserAdapter</code>:</p> <pre><code>from fastapi_fullauth.adapters.base import AbstractUserAdapter\nfrom fastapi_fullauth.types import CreateUserSchema, RefreshToken, UserSchema\n\nclass MongoAdapter(AbstractUserAdapter):\n    async def get_user_by_id(self, user_id: str) -&gt; UserSchema | None:\n        ...\n\n    async def get_user_by_email(self, email: str) -&gt; UserSchema | None:\n        ...\n\n    async def create_user(self, data: CreateUserSchema, hashed_password: str) -&gt; UserSchema:\n        ...\n\n    # ... implement all abstract methods\n</code></pre> <p>See the source of AbstractUserAdapter for the full interface.</p>"},{"location":"adapters/#auto-derived-schemas","title":"Auto-derived schemas","text":"<p>All adapters support automatic schema derivation. When you add custom fields to your user model, fastapi-fullauth detects them and includes them in:</p> <ul> <li>Registration schema \u2014 extra fields appear in <code>POST /auth/register</code></li> <li>User response schema \u2014 extra fields appear in <code>GET /auth/me</code> and other user responses</li> </ul> <p>No need to create separate Pydantic models. You can still pass explicit <code>user_schema</code> or <code>create_user_schema</code> to <code>SQLModelAdapter</code> / <code>FullAuth</code> if you want full control.</p>"},{"location":"adapters/memory/","title":"In-Memory Adapter","text":"<p>A simple adapter that stores everything in Python dictionaries. Useful for testing and quick prototyping.</p> <p>Warning</p> <p>All data is lost when the process restarts. Do not use in production.</p>"},{"location":"adapters/memory/#usage","title":"Usage","text":"<pre><code>from fastapi_fullauth import FullAuth\nfrom fastapi_fullauth.adapters.memory import InMemoryAdapter\n\nfullauth = FullAuth(\n    secret_key=\"dev-secret\",\n    adapter=InMemoryAdapter(),\n)\n</code></pre> <p>No database, no migrations, no setup. Just works.</p>"},{"location":"adapters/memory/#when-to-use","title":"When to use","text":"<ul> <li>Unit tests \u2014 all fastapi-fullauth tests use this adapter</li> <li>Prototyping \u2014 try out the library without setting up a database</li> <li>Demos \u2014 run the <code>examples/memory_app</code> example</li> </ul>"},{"location":"adapters/memory/#limitations","title":"Limitations","text":"<ul> <li>No persistence across restarts</li> <li>No concurrent access safety (single-process only)</li> <li>OAuth account methods are supported but also in-memory</li> </ul>"},{"location":"adapters/sqlalchemy/","title":"SQLAlchemy Adapter","text":"<p>Use this adapter if your project already uses SQLAlchemy's declarative base.</p>"},{"location":"adapters/sqlalchemy/#installation","title":"Installation","text":"<pre><code>pip install fastapi-fullauth[sqlalchemy]\n</code></pre>"},{"location":"adapters/sqlalchemy/#setup","title":"Setup","text":""},{"location":"adapters/sqlalchemy/#1-define-your-user-model","title":"1. Define your user model","text":"<pre><code>from sqlalchemy import Boolean, Column, DateTime, String, Table, ForeignKey\nfrom sqlalchemy.orm import DeclarativeBase, Mapped, mapped_column, relationship\nfrom fastapi_fullauth.adapters.sqlalchemy.models import (\n    FullAuthBase, RoleModel, UserBase, user_role_table,\n)\n\nclass User(UserBase):\n    __tablename__ = \"fullauth_users\"\n\n    # add your custom fields\n    display_name: Mapped[str] = mapped_column(String(100), default=\"\")\n    phone: Mapped[str] = mapped_column(String(20), default=\"\")\n\n    # required relationships\n    roles: Mapped[list[RoleModel]] = relationship(secondary=user_role_table)\n</code></pre> <p><code>UserBase</code> provides the same core fields as the SQLModel version: <code>id</code>, <code>email</code>, <code>hashed_password</code>, <code>is_active</code>, <code>is_verified</code>, <code>is_superuser</code>, <code>created_at</code>.</p>"},{"location":"adapters/sqlalchemy/#2-create-the-adapter","title":"2. Create the adapter","text":"<pre><code>from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine\nfrom fastapi_fullauth.adapters.sqlalchemy import SQLAlchemyAdapter\n\nengine = create_async_engine(\"sqlite+aiosqlite:///app.db\")\nsession_maker = async_sessionmaker(engine, expire_on_commit=False)\n\nadapter = SQLAlchemyAdapter(session_maker=session_maker, user_model=User)\n</code></pre>"},{"location":"adapters/sqlalchemy/#3-wire-into-fullauth","title":"3. Wire into FullAuth","text":"<pre><code>from fastapi_fullauth import FullAuth\n\nfullauth = FullAuth(\n    secret_key=\"your-secret-key\",\n    adapter=adapter,\n)\n</code></pre>"},{"location":"adapters/sqlalchemy/#table-creation","title":"Table creation","text":"<p>Use your existing Alembic setup or create tables directly:</p> <pre><code>async with engine.begin() as conn:\n    await conn.run_sync(FullAuthBase.metadata.create_all)\n</code></pre>"},{"location":"adapters/sqlalchemy/#schema-derivation","title":"Schema derivation","text":"<p>The SQLAlchemy adapter auto-derives schemas from your model's column definitions. Column types are mapped to Python types:</p> SQLAlchemy Type Python Type <code>String</code>, <code>Text</code> <code>str</code> <code>Integer</code> <code>int</code> <code>Float</code>, <code>Numeric</code> <code>float</code> <code>Boolean</code> <code>bool</code>"},{"location":"adapters/sqlmodel/","title":"SQLModel Adapter","text":"<p>The recommended adapter for most projects.</p>"},{"location":"adapters/sqlmodel/#installation","title":"Installation","text":"<pre><code>pip install fastapi-fullauth[sqlmodel]\n</code></pre>"},{"location":"adapters/sqlmodel/#setup","title":"Setup","text":""},{"location":"adapters/sqlmodel/#1-define-your-user-model","title":"1. Define your user model","text":"<pre><code>from sqlmodel import Field, Relationship\nfrom fastapi_fullauth.adapters.sqlmodel import (\n    UserBase, Role, UserRoleLink, RefreshTokenRecord,\n)\n\nclass User(UserBase, table=True):\n    __tablename__ = \"fullauth_users\"\n\n    # add your custom fields\n    display_name: str = Field(default=\"\", max_length=100)\n    phone: str = Field(default=\"\", max_length=20)\n\n    # required relationships\n    roles: list[Role] = Relationship(link_model=UserRoleLink)\n    refresh_tokens: list[RefreshTokenRecord] = Relationship()\n</code></pre> <p><code>UserBase</code> provides these fields:</p> Field Type Description <code>id</code> <code>UUID</code> (UUID7) Primary key, auto-generated <code>email</code> <code>str</code> Unique, indexed <code>hashed_password</code> <code>str</code> Password hash <code>is_active</code> <code>bool</code> Account active flag <code>is_verified</code> <code>bool</code> Email verified flag <code>is_superuser</code> <code>bool</code> Superuser flag <code>created_at</code> <code>datetime</code> UTC creation timestamp"},{"location":"adapters/sqlmodel/#2-create-the-adapter","title":"2. Create the adapter","text":"<pre><code>from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine\n\nengine = create_async_engine(\"sqlite+aiosqlite:///app.db\")\nsession_maker = async_sessionmaker(engine, expire_on_commit=False)\n</code></pre> <p>You can use either SQLAlchemy's <code>AsyncSession</code> or SQLModel's <code>AsyncSession</code>:</p> SQLAlchemy AsyncSessionSQLModel AsyncSession <pre><code>from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine\n\nengine = create_async_engine(\"sqlite+aiosqlite:///app.db\")\nsession_maker = async_sessionmaker(engine, expire_on_commit=False)\n</code></pre> <pre><code>from sqlalchemy.ext.asyncio import create_async_engine\nfrom sqlalchemy.ext.asyncio import async_sessionmaker\nfrom sqlmodel.ext.asyncio.session import AsyncSession\n\nengine = create_async_engine(\"sqlite+aiosqlite:///app.db\")\nsession_maker = async_sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)\n</code></pre> <p>Then create the adapter:</p> <pre><code>from fastapi_fullauth.adapters.sqlmodel import SQLModelAdapter\n\nadapter = SQLModelAdapter(session_maker=session_maker, user_model=User)\n</code></pre>"},{"location":"adapters/sqlmodel/#3-wire-into-fullauth","title":"3. Wire into FullAuth","text":"<pre><code>from fastapi_fullauth import FullAuth\n\nfullauth = FullAuth(\n    secret_key=\"your-secret-key\",\n    adapter=adapter,\n)\n</code></pre>"},{"location":"adapters/sqlmodel/#tables-created","title":"Tables created","text":"<p>The SQLModel adapter uses these tables:</p> Table Purpose <code>fullauth_users</code> User accounts (your model) <code>fullauth_roles</code> Role definitions <code>fullauth_user_roles</code> User-role link table <code>fullauth_refresh_tokens</code> Stored refresh tokens <code>fullauth_oauth_accounts</code> Linked OAuth provider accounts"},{"location":"adapters/sqlmodel/#custom-user-schema","title":"Custom user schema","text":"<p>By default, the response schema is auto-derived from your model. To use an explicit schema:</p> <pre><code>from pydantic import BaseModel, EmailStr\nfrom fastapi_fullauth.types import UserSchema\n\nclass MyUserResponse(UserSchema):\n    display_name: str = \"\"\n    phone: str = \"\"\n\nadapter = SQLModelAdapter(\n    session_maker=session_maker,\n    user_model=User,\n    user_schema=MyUserResponse,\n)\n</code></pre>"},{"location":"adapters/sqlmodel/#oauth-support","title":"OAuth support","text":"<p>The SQLModel adapter includes full OAuth support. The <code>OAuthAccountRecord</code> model is included and the adapter implements all OAuth methods from <code>AbstractUserAdapter</code>.</p>"},{"location":"auth/custom-claims/","title":"Custom Token Claims","text":"<p>Embed app-specific data into JWT tokens. Custom claims are available in the <code>extra</code> field of decoded token payloads.</p>"},{"location":"auth/custom-claims/#setup","title":"Setup","text":"<p>Pass an async callback to <code>on_create_token_claims</code>:</p> <pre><code>from fastapi_fullauth.types import UserSchema\n\nasync def add_claims(user: UserSchema) -&gt; dict:\n    return {\n        \"tenant_id\": \"acme\",\n        \"plan\": \"pro\",\n    }\n\nfullauth = FullAuth(\n    secret_key=\"...\",\n    adapter=adapter,\n    on_create_token_claims=add_claims,\n)\n</code></pre> <p>The returned dict is embedded in the <code>extra</code> field of every access token.</p>"},{"location":"auth/custom-claims/#accessing-claims","title":"Accessing claims","text":"<p>Custom claims are available when decoding tokens:</p> <pre><code>payload = await fullauth.token_engine.decode_token(token)\ntenant_id = payload.extra.get(\"tenant_id\")\nplan = payload.extra.get(\"plan\")\n</code></pre>"},{"location":"auth/custom-claims/#reserved-keys","title":"Reserved keys","text":"<p>The following keys cannot be used in custom claims (they're used by the JWT structure):</p> <p><code>sub</code>, <code>exp</code>, <code>iat</code>, <code>jti</code>, <code>type</code>, <code>roles</code>, <code>extra</code>, <code>family_id</code></p> <p>If your callback returns any of these, a <code>ValueError</code> is raised at token creation time.</p>"},{"location":"auth/custom-claims/#when-claims-are-generated","title":"When claims are generated","text":"<p>Custom claims are generated on:</p> <ul> <li>Login \u2014 embedded in the access token</li> <li>Token refresh \u2014 regenerated from the current user state</li> <li>OAuth callback \u2014 embedded after OAuth user creation/linking</li> </ul> <p>This means claims stay fresh on each refresh. If a user's plan changes, the next token refresh picks it up.</p>"},{"location":"auth/dependencies/","title":"Protected Routes","text":"<p>fastapi-fullauth provides FastAPI dependencies to protect your routes. Use them with <code>Depends()</code> or the <code>Annotated</code> type aliases.</p>"},{"location":"auth/dependencies/#dependency-types","title":"Dependency types","text":""},{"location":"auth/dependencies/#currentuser","title":"CurrentUser","text":"<p>Any authenticated user (active account required).</p> <pre><code>from fastapi_fullauth.dependencies import CurrentUser\n\n@app.get(\"/profile\")\nasync def profile(user: CurrentUser):\n    return {\"email\": user.email, \"roles\": user.roles}\n</code></pre>"},{"location":"auth/dependencies/#verifieduser","title":"VerifiedUser","text":"<p>Authenticated user with a verified email address.</p> <pre><code>from fastapi_fullauth.dependencies import VerifiedUser\n\n@app.get(\"/dashboard\")\nasync def dashboard(user: VerifiedUser):\n    return {\"email\": user.email}\n</code></pre> <p>Returns <code>403 Forbidden</code> if the user's email is not verified.</p>"},{"location":"auth/dependencies/#superuser","title":"SuperUser","text":"<p>Authenticated user with <code>is_superuser=True</code>.</p> <pre><code>from fastapi_fullauth.dependencies import SuperUser\n\n@app.delete(\"/admin/users/{user_id}\")\nasync def delete_user(user_id: str, admin: SuperUser):\n    ...\n</code></pre> <p>Returns <code>403 Forbidden</code> if the user is not a superuser.</p>"},{"location":"auth/dependencies/#require_role","title":"require_role","text":"<p>Check that the user has at least one of the specified roles. Superusers bypass all role checks.</p> <pre><code>from fastapi import Depends\nfrom fastapi_fullauth.dependencies import require_role\n\n@app.get(\"/editor\")\nasync def editor_panel(user=Depends(require_role(\"editor\"))):\n    return {\"msg\": \"welcome, editor\"}\n\n# multiple roles \u2014 user needs at least one\n@app.get(\"/content\")\nasync def content(user=Depends(require_role(\"editor\", \"author\"))):\n    return {\"msg\": \"welcome\"}\n</code></pre>"},{"location":"auth/dependencies/#require_permission","title":"require_permission","text":"<p>Check that the user has at least one of the specified permissions. Permissions are resolved through roles \u2014 a user with role <code>\"editor\"</code> gets all permissions assigned to that role.</p> <pre><code>from fastapi import Depends\nfrom fastapi_fullauth.dependencies import require_permission\n\n@app.delete(\"/posts/{id}\")\nasync def delete_post(id: str, user=Depends(require_permission(\"posts:delete\"))):\n    ...\n\n# multiple permissions \u2014 user needs at least one\n@app.put(\"/posts/{id}\")\nasync def edit_post(id: str, user=Depends(require_permission(\"posts:edit\", \"posts:admin\"))):\n    ...\n</code></pre> <p>Superusers bypass all permission checks.</p>"},{"location":"auth/dependencies/#setting-up-permissions","title":"Setting up permissions","text":"<p>Permissions are assigned to roles, not directly to users:</p> <pre><code># Assign permissions to a role (superuser only)\ncurl -X POST http://localhost:8000/api/v1/auth/admin/assign-permission \\\n  -H \"Authorization: Bearer &lt;superuser-token&gt;\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"role\": \"editor\", \"permission\": \"posts:create\"}'\n\ncurl -X POST http://localhost:8000/api/v1/auth/admin/assign-permission \\\n  -H \"Authorization: Bearer &lt;superuser-token&gt;\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"role\": \"editor\", \"permission\": \"posts:edit\"}'\n\n# List permissions for a role\ncurl http://localhost:8000/api/v1/auth/admin/role-permissions/editor \\\n  -H \"Authorization: Bearer &lt;superuser-token&gt;\"\n# \u2192 [\"posts:create\", \"posts:edit\"]\n</code></pre> <p>Or programmatically:</p> <pre><code>await adapter.assign_permission_to_role(\"editor\", \"posts:create\")\nawait adapter.assign_permission_to_role(\"editor\", \"posts:edit\")\nawait adapter.remove_permission_from_role(\"editor\", \"posts:create\")\n\n# resolve all permissions for a user (through their roles)\nperms = await adapter.get_user_permissions(user.id)\n# \u2192 [\"posts:edit\"]\n</code></pre>"},{"location":"auth/dependencies/#require_role-vs-require_permission","title":"require_role vs require_permission","text":"<code>require_role</code> <code>require_permission</code> Checks Role names on the user Permissions resolved through roles Setup Just assign roles Assign roles + map permissions to roles Use case Simple apps (\"admin vs user\") Fine-grained access (\"can edit posts?\") Change access Modify code Update DB mappings"},{"location":"auth/dependencies/#how-it-works","title":"How it works","text":"<p>All dependencies follow the same flow:</p> <ol> <li>Extract the JWT from the <code>Authorization: Bearer &lt;token&gt;</code> header (or cookie backend)</li> <li>Decode and validate the token (expiry, blacklist, signature)</li> <li>Look up the user by <code>sub</code> (user ID) from the token payload</li> <li>Apply additional checks (verified, superuser, roles)</li> </ol> <p>If any step fails, a <code>401 Unauthorized</code> or <code>403 Forbidden</code> response is returned automatically.</p>"},{"location":"auth/dependencies/#using-with-the-function-form","title":"Using with the function form","text":"<p>If you prefer the function form over <code>Annotated</code> types:</p> <pre><code>from fastapi import Depends\nfrom fastapi_fullauth.dependencies import current_user, current_active_verified_user, current_superuser\n\n@app.get(\"/profile\")\nasync def profile(user=Depends(current_user)):\n    return user\n</code></pre>"},{"location":"auth/dependencies/#role-management","title":"Role management","text":"<p>Roles are managed through the admin endpoints (superuser only):</p> <pre><code># Assign a role\ncurl -X POST http://localhost:8000/api/v1/auth/admin/assign-role \\\n  -H \"Authorization: Bearer &lt;superuser-token&gt;\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"user_id\": \"...\", \"role\": \"editor\"}'\n\n# Remove a role\ncurl -X POST http://localhost:8000/api/v1/auth/admin/remove-role \\\n  -H \"Authorization: Bearer &lt;superuser-token&gt;\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"user_id\": \"...\", \"role\": \"editor\"}'\n</code></pre> <p>You can also manage roles programmatically through the adapter:</p> <pre><code>await adapter.assign_role(user_id, \"editor\")\nawait adapter.remove_role(user_id, \"editor\")\nroles = await adapter.get_user_roles(user_id)\n</code></pre>"},{"location":"auth/hooks/","title":"Event Hooks","text":"<p>Hooks let you run custom logic when auth events happen - send emails, log analytics, sync with external systems - without modifying the core auth flows.</p>"},{"location":"auth/hooks/#registering-hooks","title":"Registering hooks","text":"<pre><code>fullauth = FullAuth(secret_key=\"...\", adapter=adapter)\n\nasync def on_register(user):\n    print(f\"New user: {user.email}\")\n\nfullauth.hooks.on(\"after_register\", on_register)\n</code></pre>"},{"location":"auth/hooks/#available-events","title":"Available events","text":""},{"location":"auth/hooks/#user-lifecycle","title":"User lifecycle","text":"Event Callback signature When <code>after_register</code> <code>async def(user: UserSchema)</code> After successful registration <code>after_login</code> <code>async def(user: UserSchema)</code> After successful login <code>after_logout</code> <code>async def(user_id: str)</code> After logout <code>after_password_change</code> <code>async def(user: UserSchema)</code> After password change <code>after_password_reset</code> <code>async def(user: UserSchema)</code> After password reset <code>after_email_verify</code> <code>async def(user: UserSchema)</code> After email verification"},{"location":"auth/hooks/#email-events","title":"Email events","text":"Event Callback signature When <code>send_verification_email</code> <code>async def(email: str, token: str)</code> When verification is requested <code>send_password_reset_email</code> <code>async def(email: str, token: str)</code> When password reset is requested"},{"location":"auth/hooks/#oauth-events","title":"OAuth events","text":"Event Callback signature When <code>after_oauth_login</code> <code>async def(user: UserSchema, provider: str, is_new_user: bool)</code> After OAuth callback"},{"location":"auth/hooks/#example-email-verification","title":"Example: email verification","text":"<pre><code>async def send_verification_email(email: str, token: str):\n    # build your verification URL\n    verify_url = f\"https://myapp.com/verify?token={token}\"\n    await my_email_service.send(\n        to=email,\n        subject=\"Verify your email\",\n        body=f\"Click here to verify: {verify_url}\",\n    )\n\nasync def send_password_reset_email(email: str, token: str):\n    reset_url = f\"https://myapp.com/reset-password?token={token}\"\n    await my_email_service.send(\n        to=email,\n        subject=\"Reset your password\",\n        body=f\"Click here to reset: {reset_url}\",\n    )\n\nfullauth.hooks.on(\"send_verification_email\", send_verification_email)\nfullauth.hooks.on(\"send_password_reset_email\", send_password_reset_email)\n</code></pre> <p>Note</p> <p>If you don't register a <code>send_verification_email</code> hook, the verification token is still generated but never delivered. Same for password reset.</p>"},{"location":"auth/hooks/#example-audit-logging","title":"Example: audit logging","text":"<pre><code>import logging\n\nlogger = logging.getLogger(\"auth\")\n\nasync def log_login(user):\n    logger.info(f\"Login: {user.email} (id={user.id})\")\n\nasync def log_failed_logout(user_id):\n    logger.info(f\"Logout: user_id={user_id}\")\n\nfullauth.hooks.on(\"after_login\", log_login)\nfullauth.hooks.on(\"after_logout\", log_failed_logout)\n</code></pre>"},{"location":"auth/hooks/#multiple-hooks-per-event","title":"Multiple hooks per event","text":"<p>You can register multiple callbacks for the same event. They run in registration order:</p> <pre><code>fullauth.hooks.on(\"after_register\", send_welcome_email)\nfullauth.hooks.on(\"after_register\", create_default_workspace)\nfullauth.hooks.on(\"after_register\", track_signup_analytics)\n</code></pre>"},{"location":"auth/passwords/","title":"Password Validation","text":"<p>fastapi-fullauth includes a configurable password validator that checks passwords on registration, password change, and password reset.</p>"},{"location":"auth/passwords/#default-behavior","title":"Default behavior","text":"<p>By default, only minimum length is enforced (8 characters, configurable via <code>PASSWORD_MIN_LENGTH</code>).</p>"},{"location":"auth/passwords/#custom-rules","title":"Custom rules","text":"<pre><code>from fastapi_fullauth import FullAuth\nfrom fastapi_fullauth.validators import PasswordValidator\n\nvalidator = PasswordValidator(\n    min_length=10,\n    require_uppercase=True,\n    require_lowercase=True,\n    require_digit=True,\n    require_special=True,\n    blocked_passwords=[\"password123\", \"qwerty123\"],\n)\n\nfullauth = FullAuth(\n    secret_key=\"...\",\n    adapter=adapter,\n    password_validator=validator,\n)\n</code></pre>"},{"location":"auth/passwords/#validation-rules","title":"Validation rules","text":"Rule Default Description <code>min_length</code> <code>8</code> Minimum password length <code>require_uppercase</code> <code>False</code> Must contain <code>[A-Z]</code> <code>require_lowercase</code> <code>False</code> Must contain <code>[a-z]</code> <code>require_digit</code> <code>False</code> Must contain <code>[0-9]</code> <code>require_special</code> <code>False</code> Must contain <code>[!@#$%^&amp;*(),.?\":{}|&lt;&gt;]</code> <code>blocked_passwords</code> <code>[]</code> List of disallowed passwords (case-insensitive) <p>When validation fails, a <code>422 Unprocessable Entity</code> response is returned with all violated rules:</p> <pre><code>{\n  \"detail\": \"Password must be at least 10 characters; Password must contain at least one uppercase letter\"\n}\n</code></pre>"},{"location":"auth/passwords/#password-hashing","title":"Password hashing","text":"<p>Passwords are hashed with Argon2id by default. Switch to bcrypt via config:</p> <pre><code>fullauth = FullAuth(\n    secret_key=\"...\",\n    adapter=adapter,\n    password_hash_algorithm=\"bcrypt\",  # requires: pip install bcrypt\n)\n</code></pre> <p>When switching algorithms, existing hashes are transparently detected by prefix (<code>$2b$</code> for bcrypt, <code>$argon2</code> for Argon2id). Users are rehashed on their next successful login.</p>"},{"location":"security/middleware/","title":"Middleware","text":"<p>fastapi-fullauth includes three middleware components. By default, <code>init_app()</code> auto-wires them based on config flags. Pass <code>auto_middleware=False</code> to manage them yourself.</p> <pre><code>fullauth.init_app(app, auto_middleware=False)\n</code></pre>"},{"location":"security/middleware/#security-headers","title":"Security Headers","text":"<p>Enabled by default (<code>INJECT_SECURITY_HEADERS=True</code>). Adds standard security headers to every response:</p> Header Value <code>X-Content-Type-Options</code> <code>nosniff</code> <code>X-Frame-Options</code> <code>DENY</code> <code>X-XSS-Protection</code> <code>1; mode=block</code> <code>Strict-Transport-Security</code> <code>max-age=31536000; includeSubDomains</code> <code>Referrer-Policy</code> <code>strict-origin-when-cross-origin</code> <code>Permissions-Policy</code> <code>geolocation=(), camera=(), microphone=()</code>"},{"location":"security/middleware/#custom-headers","title":"Custom headers","text":"<p>Override or add headers:</p> <pre><code>from fastapi_fullauth.middleware import SecurityHeadersMiddleware\n\napp.add_middleware(\n    SecurityHeadersMiddleware,\n    custom_headers={\n        \"X-Frame-Options\": \"SAMEORIGIN\",  # override default\n        \"X-Custom-Header\": \"value\",       # add new\n    },\n)\n</code></pre>"},{"location":"security/middleware/#csrf-protection","title":"CSRF Protection","text":"<p>Disabled by default (<code>CSRF_ENABLED=False</code>). Enable it for cookie-based auth where the frontend and backend share a domain:</p> <pre><code>fullauth = FullAuth(\n    secret_key=\"...\",\n    adapter=adapter,\n    csrf_enabled=True,\n    csrf_secret=\"optional-separate-secret\",  # falls back to SECRET_KEY\n)\n</code></pre>"},{"location":"security/middleware/#how-it-works","title":"How it works","text":"<p>Uses the double-submit cookie pattern:</p> <ol> <li>On <code>GET</code> requests, a signed CSRF cookie (<code>fullauth_csrf</code>) is set</li> <li>On state-changing requests (<code>POST</code>, <code>PUT</code>, <code>PATCH</code>, <code>DELETE</code>), the client must send the cookie value in the <code>X-CSRF-Token</code> header</li> <li>The middleware verifies the cookie signature and compares cookie vs header</li> </ol>"},{"location":"security/middleware/#frontend-integration","title":"Frontend integration","text":"<pre><code>// read the CSRF cookie\nconst csrfToken = document.cookie\n  .split('; ')\n  .find(row =&gt; row.startsWith('fullauth_csrf='))\n  ?.split('=')[1];\n\n// include it in requests\nfetch('/api/v1/auth/login', {\n  method: 'POST',\n  headers: {\n    'Content-Type': 'application/json',\n    'X-CSRF-Token': csrfToken,\n  },\n  credentials: 'include',\n  body: JSON.stringify({ email, password }),\n});\n</code></pre>"},{"location":"security/middleware/#exempt-paths","title":"Exempt paths","text":"<pre><code>from fastapi_fullauth.middleware import CSRFMiddleware\n\napp.add_middleware(\n    CSRFMiddleware,\n    secret=\"your-secret\",\n    exempt_paths=[\"/api/v1/webhooks\"],\n)\n</code></pre>"},{"location":"security/middleware/#rate-limiting","title":"Rate Limiting","text":"<p>See Rate Limiting for full details.</p>"},{"location":"security/rate-limiting/","title":"Rate Limiting","text":"<p>fastapi-fullauth provides two levels of rate limiting:</p> <ol> <li>Auth rate limits \u2014 per-route limits on login, register, and password reset (enabled by default)</li> <li>Global rate limit middleware \u2014 limits all requests per IP (disabled by default)</li> </ol> <p>Both support in-memory and Redis backends.</p>"},{"location":"security/rate-limiting/#auth-rate-limits","title":"Auth rate limits","text":"<p>Enabled by default. Protects auth endpoints from brute force:</p> Route Default limit Config Login 5 per minute <code>AUTH_RATE_LIMIT_LOGIN</code> Register 3 per minute <code>AUTH_RATE_LIMIT_REGISTER</code> Password reset 3 per minute <code>AUTH_RATE_LIMIT_PASSWORD_RESET</code> <pre><code>fullauth = FullAuth(\n    secret_key=\"...\",\n    adapter=adapter,\n    auth_rate_limit_login=10,           # 10 login attempts per window\n    auth_rate_limit_register=5,         # 5 registrations per window\n    auth_rate_limit_window_seconds=120, # 2-minute window\n)\n</code></pre> <p>Disable entirely:</p> <pre><code>fullauth = FullAuth(\n    secret_key=\"...\",\n    adapter=adapter,\n    auth_rate_limit_enabled=False,\n)\n</code></pre>"},{"location":"security/rate-limiting/#global-rate-limit-middleware","title":"Global rate limit middleware","text":"<p>Limits all requests per client IP. Disabled by default:</p> <pre><code>fullauth = FullAuth(\n    secret_key=\"...\",\n    adapter=adapter,\n    rate_limit_enabled=True,\n)\nfullauth.init_app(app)  # middleware is auto-added\n</code></pre> <p>Default: 60 requests per 60 seconds per IP.</p> <p>Response headers are included on every response:</p> <pre><code>X-RateLimit-Limit: 60\nX-RateLimit-Remaining: 57\nX-RateLimit-Reset: 45\n</code></pre> <p>When the limit is exceeded, a <code>429 Too Many Requests</code> response is returned.</p>"},{"location":"security/rate-limiting/#proxy-support","title":"Proxy support","text":"<p>Behind a reverse proxy (Nginx, Cloudflare, AWS ALB), <code>request.client.host</code> is the proxy's IP, not the real user's. Configure trusted proxy headers so rate limiting works correctly:</p> <pre><code>fullauth = FullAuth(\n    secret_key=\"...\",\n    adapter=adapter,\n    trusted_proxy_headers=[\"X-Forwarded-For\"],\n)\n</code></pre> <p>Warning</p> <p>Only list headers you trust. If your server is directly exposed to the internet (no proxy), leave this empty \u2014 otherwise users can spoof their IP via the header.</p> <p>When <code>X-Forwarded-For</code> contains a chain (e.g. <code>1.2.3.4, 10.0.0.1</code>), the first IP (original client) is used.</p> <p>This setting applies to both auth rate limits and the global rate limit middleware.</p>"},{"location":"security/rate-limiting/#redis-backend","title":"Redis backend","text":"<p>For multi-process or multi-server deployments, use the Redis backend:</p> <pre><code>fullauth = FullAuth(\n    secret_key=\"...\",\n    adapter=adapter,\n    rate_limit_enabled=True,\n    rate_limit_backend=\"redis\",\n    redis_url=\"redis://localhost:6379/0\",\n)\n</code></pre> <p>The in-memory backend is per-process. Redis shares state across all workers/servers.</p>"},{"location":"security/rate-limiting/#manual-middleware-setup","title":"Manual middleware setup","text":"<p>If you need more control, disable auto-middleware and add it yourself:</p> <pre><code>from fastapi_fullauth.protection.ratelimit import RateLimitMiddleware, RateLimiter\n\nfullauth.init_app(app, auto_middleware=False)\n\napp.add_middleware(\n    RateLimitMiddleware,\n    max_requests=100,\n    window_seconds=60,\n    exempt_paths=[\"/health\", \"/metrics\"],\n    trusted_proxy_headers=[\"X-Forwarded-For\"],\n)\n</code></pre>"}]}